The var datatype encompasses all datatypes

it is layout like this:

var
 |- Type (enum for all types)
 |- Data

vars are to make arrays because the type field of the var datatype
can get the size of the data in bytes so that indexing can be done.